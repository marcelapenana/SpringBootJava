package org.aguzman.hibernateapp.entity;

public class Anotaciones {

    /*

     DAO o Data Access Object estamos hablando de un patrón de diseño muy clásico en
      el cual una clase se encarga de las operaciones de persistencia contra una tabla de la base de datos.
      Esa clase normalmente dispone de un conjunto de métodos que se encargan de salvar borrar, editar y actualizar la tabla de la bases de datos .
     //Relacion de tablas y tablas intermedias
    //Iniciamos add clases no relacionadas

    hibernate:herramienta de mapeo de objeto relacional, objetos elacionados
    unos contros, estas entidades son objetos de java con atributos
    que estan mapeados asignados a una tabla donde cada atributo de esta clase se
    asigna a un campo a la tabla de la base de datos es decir se mapea,
    automatizamos trabajando con objetos y notaciones

    DAO  acceden y operan con los datos mediante hibernate,
    jpa es el api de persistencia de java con el objeto entityu manager
    el core o motor de hibernate funciona  con jdb.
    Las consultas se realizan con jdbc y
    todo el mapeo es automatico
    El api ya sea hibernate o jpa, se realiza una consulta u operacion luego
    que pasa por hibernate, realiza la consulta a jdbc correspondiente y a la
    bd, se devulkeven datos y los mapea a los objertos entitys
    */
}
